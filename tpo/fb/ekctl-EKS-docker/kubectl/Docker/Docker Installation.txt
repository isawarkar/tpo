sudo apt-get update
sudo apt-get install \
    ca-certificates \
    curl \
    gnupg \
    lsb-release -y
sudo mkdir -p /etc/apt/keyrings	
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
  
sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin -y
systemctl start docker
systemctl status docker
systemctl enable docker
#################################Dockerfile######################################
FROM ubuntu:20.04
RUN mkdir /opt/tomcat/
RUN apt update
RUN apt install curl -y
RUN apt install openjdk-11-jdk -y
RUN apt install unzip -y
WORKDIR /tmp
ADD https://dlcdn.apache.org/tomcat/tomcat-9/v9.0.64/bin/apache-tomcat-9.0.64.zip .
RUN unzip apache*.zip
RUN mv apache-tomcat-9.0.64/* /opt/tomcat
RUN ls -l
#WORKDIR /opt/tomcat/webapps
#RUN curl -O -L https://app-online-exam.s3.ap-south-1.amazonaws.com/exam.war
COPY exam.war /opt/tomcat/webapps/
#COPY exam /opt/tomcat/webapps/exam/
COPY index.jsp /opt/tomcat/webapps/ROOT/
WORKDIR /opt/tomcat/webapps
RUN ls -l
WORKDIR /opt/tomcat/bin
#RUN chown -R tomcat: /opt/tomcat
RUN sh -c 'chmod +x /opt/tomcat/bin/*.sh'
EXPOSE 8080
CMD ["/opt/tomcat/bin/catalina.sh", "run"]
####################################################################################
docker build -t fb . //Docker build images
docker run -d --name fb -p 8080:8080 fb //Docker run images

docker container logs fb //to see the logs
docker kill fb //TO kill COntainer
docker rm fb//Delete Container
docker rmi imageName//Delete Image

#####################################################################################3
sudo cp -p /etc/ssh/sshd_config /etc/ssh/sshd_config_backup;
sudo sed -i 's/^PasswordAuthentication.*/PasswordAuthentication yes/' /etc/ssh/sshd_config;
sudo sed -i 's/^PermitRootLogin*/PermitRootLogin yes/' /etc/ssh/sshd_config;
sudo echo "PermitRootLogin yes" >> /etc/ssh/sshd_config;
sudo systemctl restart sshd;
sudo adduser indra

usermod -G root indra
usermod -G 0 -o -u indra
passwd indrajeet // TO change pass

Method 3: Editing /etc/passwd file
Edit /etc/passwd for the particular user. Change the user's UID and GID to '0'. This will give root permissions to user.
#################################################Create Cert
sudo certbot certonly --standalone -d www.fresherbuddy.in,fresherbuddy.in,uddanda.fresherbuddy.in,onlinetest.fresherbuddy.in
sudo -s
cd /etc/letsencrypt/live/www.fresherbuddy.in
cp cert.pem /opt/tomcat/apache-tomcat-9.0.54/conf
cp chain.pem /opt/tomcat/apache-tomcat-9.0.54/conf
cp privkey.pem /opt/tomcat/apache-tomcat-9.0.54/conf



