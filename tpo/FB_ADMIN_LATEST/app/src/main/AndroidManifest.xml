<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Phone -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Legacy storage permissions -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Alarm -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- SMS -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup,android:icon,android:theme">

        <!-- Launcher -->
        <activity
            android:name="com.ut.fbn.admin.SplashScreenActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/SplashScreenTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name="com.ut.fbn.admin.MainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.ut.fbn.admin.LoginActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.ut.fbn.admin.AboutActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.ut.fbn.admin.ChangePasswordActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.ut.fbn.admin.ForgotPasswordActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.ut.fbn.admin.ScanQRCodeActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.ut.fbn.admin.CompanyDetails"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.ut.fbn.admin.CompanyList"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.ut.fbn.admin.OpeningListActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.ut.fbn.admin.StudentListActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.ut.fbn.admin.FullProfileImageActivity"
            android:screenOrientation="portrait" />

        <!-- Notification receiver -->
        <receiver
            android:name="com.ut.fbn.admin.receiver.NoticeReceiver"
            android:exported="false" />

        <!-- Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- AndroidX FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/providerpaths" />

        </provider>

    </application>

</manifest>